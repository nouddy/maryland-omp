#include <ysilib\YSI_Coding\y_hooks>

hook OnGameModeInit()
{
	print("name/name.extension loaded");


    return 1;
}

hook OnPlayerConnect(playerid)
{

	return 1;
}

/*
	if(help)
    {
        SendClientMessage(playerid, x_blue, "HELP >> "c_white"Komanda koja vam .");
		return 1;
    }

	if (player_Staff[playerid] < 1)
		return SendClientMessage(playerid, -1, ""c_server"montrey // "c_white"Samo Staff Team moze ovo.");

*/